package com.example.ubernet.repository;

import com.example.ubernet.model.RideDenial;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RideDenialRepository extends JpaRepository<RideDenial, Long> {
}
