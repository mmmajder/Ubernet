package com.example.ubernet.repository;

import com.example.ubernet.model.PathAlternative;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PathAlternativeRepository extends JpaRepository<PathAlternative, Long> {
}
